CREATE TABLE driver (
driver_id VARCHAR(250) PRIMARY KEY,
driver_name VARCHAR(250) NOT NULL,
driver_address VARCHAR(250) NOT NULL,
driver_car_no VARCHAR(250) NOT NULL,
driver_car_status VARCHAR(250) NOT NULL,
driver_name VARCHAR(250) NOT NULL,
created_at Date,
updated_date Date,
mobile_no INT NOT NULL,
);